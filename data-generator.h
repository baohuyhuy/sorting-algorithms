#pragma once

#include <vector>

vector<int> generateRandomData(int);

vector<int> generateNearlySortedData(int);

vector<int> generateSortedData(int);

vector<int> generateReverseData(int);

vector<int> generateData(int, int);